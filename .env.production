# 页面标题
VITE_APP_TITLE = USDT收银台系统

# 生产环境配置
VITE_APP_ENV = 'production'

# === 核心修改在这里 ===

# 1. 主业务接口（对应 48080 端口）
# 保持原样，通常若依默认为 /prod-api
VITE_APP_BASE_API = '/api-remote'

# 2. 第二个业务接口（对应 7120 端口）
# 新增这个变量，名字你可以随便取，比如 VITE_APP_BIZ_API
VITE_APP_BIZ_API = '/api-local'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip